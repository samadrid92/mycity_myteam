
<% @cities.each do |c| %>
  <%= link_to "#{c.city_name}", city_path(c) %>
  <%= c.date_founded %>
  <%= c.population %>
  <%= c.nicknames %>
  <%= c.header_pic %>
  <%= link_to "Edit", edit_city_path(c) %>
<% end %>
